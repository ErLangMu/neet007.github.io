**环境变量：**
/etc/profile

查看：

```
# export
# echo $PATH
```

临时更改：

```
# PATH=$PATH:添加目录
```

添加至环境：（将普通变量转换为环境变量）

```
# export PATH
# export PATH=$PATH:添加目录
```

永久更改：

```
# echo "export PATH=$PATH:添加目录">>/etc/profile
# logout或# reboot
```
